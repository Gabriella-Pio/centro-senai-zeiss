"use client";

export function DemoBanner() {
  return (
    <div className="demo-banner" role="status">
      Modo demonstração — dados de fluxo em localStorage, separados do histórico real do laboratório.
    </div>
  );
}
